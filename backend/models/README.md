# MediaPipe model

Blink liveness uses the MediaPipe Tasks Face Landmarker model.

Place the model file at:

```text
backend/models/face_landmarker.task
```

The model binary is intentionally excluded from Git.
